# Bug 日志：<问题标题>

- 日期：YYYY-MM-DD
- 严重级别：低 / 中 / 高 / 紧急
- 影响范围：<功能模块或页面>
- 相关文件：`<path/to/file>`
- 关联提交（可选）：`<commit-hash>`

## 现象

描述用户可见的问题表现。

示例：

```text
点击 XXX 后，页面出现 XXX / 返回 XXX
```

## 复现步骤

1. 进入 `<页面或场景>`
2. 执行 `<操作1>`
3. 执行 `<操作2>`
4. 观察到 `<异常结果>`

## 预期结果

描述正确行为。

## 实际结果

描述错误行为。

## 根因分析

从代码和数据流角度说明问题根因，尽量定位到具体逻辑。

- 原因 1：...
- 原因 2（如有）：...

## 修复方案

说明采取的修复策略和关键改动点。

1. 修改 `<文件路径>`：<改动说明>
2. 修改 `<文件路径>`：<改动说明>

## 修复结果

说明问题是否完全解决，以及是否有副作用。

## 验证

- 静态检查：`npm run lint` / `npx tsc --noEmit`
- 功能验证：按复现步骤回归，确认结果正常
- 边界验证（可选）：<边界场景>

## 回归风险

列出可能受影响的相邻功能点。

- 风险点 1：...
- 风险点 2：...

## 后续行动（可选）

- [ ] 增加自动化测试：<测试点>
- [ ] 增加监控/日志：<监控点>
- [ ] 文档补充：<文档位置>

## 经验总结

沉淀可复用经验，避免同类问题再次发生。
